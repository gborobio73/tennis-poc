describe("A suite", function() {
  it("contains spec with an expectation", function() {
    var matchScore = new MatchScore(0);
    matchScore.opponentPoint();    

    expect(true).toBe(true);
  });

});
